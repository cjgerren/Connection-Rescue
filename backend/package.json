{
  "name": "connectionrescue-backend",
  "version": "1.0.0",
  "private": true,
  "description": "ConnectionRescue API server. Real Duffel flight search/booking, Stripe Checkout, and Supabase persistence. All travel API keys live here, never in the React app.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^16.12.0"
  }
}
